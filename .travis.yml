
language: python
python:
  - "3.8.3"

install:
  - pip install codecov

stages:
  - name: test

jobs:
  include:
  - stage: test
    name: Unit Test
    script: pytest --cov=./
    after_success:
      - codecov
